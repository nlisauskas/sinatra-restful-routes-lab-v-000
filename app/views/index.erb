<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Check Out All Recipes Below:</h1>
    <% @recipes.each do |recipe| %>
    <ul>
      <li><%= @recipes.name %></li>
      <li><%= @recipes.ingredients %></li>
      <li><%= @recipes.cook_time %></li>
    </ul>
    <% end %>

    <a href="/recipes/new">Make a new recipe here.</a>
  </body>
</html>
